# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# The French specification is internal: this repository is English-only.
fluidunreal.md
cahier_des_charges*.md

# Unreal: never versioned, never counted in the disk budget, only reported.
**/Saved/
**/Intermediate/
**/DerivedDataCache/
**/Binaries/
**/Build/
# No .uasset or .umap in this repository, ever: what the probes import is a build product
# of running them, not source. The test bed is a .uproject plus .ini, and its scene is
# built in code at test time.
scripts/lot0/TestBed/Content/
*.sln
.vs/

# Lot 0 raw outputs are committed on purpose; their heavy by-products are not.
docs/lot0/**/*.log.gz
